{
  "buildCommand": "cd apps/web && npm install && npm run build",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/tles", "destination": "/api/catalog" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
